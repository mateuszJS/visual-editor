import styles from './overlayStyles.module.css'

const classNamesOverlay = {
  base: styles.overlay,
  afterOpen: styles.overlayOpen,
  beforeClose: styles.overlayClosed,
}

export default classNamesOverlay
