Creator:

- on the beginning receives initial state with list of all the assets and canvas dimensions any other communication between App - Creator happens thoug methods exposes by creator. Not by providing a new list of assets
- exposes method like addImage()
- moving, rotating, aligning etc happens withing the creator, app doesn't know about it. App is only informed about final result and it is only to be sent to backend, not to by consumed by the app.
